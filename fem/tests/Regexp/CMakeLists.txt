INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( regexp.sif regexp.sif COPYONLY)
ADD_ELMERTEST_MODULE(regexp regexp regexp.F90)

file(COPY square.grd regexp.F90 ELMERSOLVER_STARTINFO DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(regexp)
ADD_ELMER_LABEL(regexp quick)
